/* exported isVowel */
function isVowel(char) {
  if (char === 'A' || char === 'E' || char === 'I' || char === 'O' || char === 'a' || char === 'e' || char === 'i' || char === 'o' || char === 'u' || char === 'U') {
    return true;
  } else {
    return false;
  }

}
